# AI Oncall Agent - 基础依赖（orchestrator + rag 共享）
# 不含 sentence-transformers / chromadb / torch（这些只在 rag-engine 用）

# FastAPI
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
httpx>=0.26.0
python-dotenv>=1.0.0
python-multipart>=0.0.6

# Rate Limiting
slowapi>=0.1.9

# RabbitMQ
pika>=1.3.0

# LLM SDK
qianfan>=0.3.0
openai>=1.10.0

# LangChain
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-core>=0.2.0

# Document Parsing（orchestrator 上传文档时用；rag 解析也用）
python-docx>=1.0.0
PyPDF2>=3.0.0

# 工具
pyyaml>=6.0

# ⚠️ 注意：grpcio / grpcio-reflection / protobuf 在 Dockerfile 中分阶段安装
# 原因：grpcio-reflection 1.81.0 还未声明对 protobuf 7.x 的支持，
# 但项目 gencode 是 protobuf 7.35.0 生成。Dockerfile 中用 --no-deps 绕开 resolver 冲突。
# （本地 .venv 已验证此组合可用）
